import React from 'react';
import PropTypes from 'prop-types';

const EmptyStae = props => {
    return (
        <div>
            
        </div>
    );
};

EmptyStae.propTypes = {
    
};

export default EmptyStae;